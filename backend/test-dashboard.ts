import { reportService } from './src/domains/core/reports/report.service.js'; import prisma from './src/config/database.js'; async function test() { const org = await prisma.organization.findFirst(); console.log('Testing org:', org.id); try { const res = await reportService.dashboardMetrics(org.id); console.log(res); } catch(e) { console.error(e); } finally { await prisma.$disconnect(); } } test();
